#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../flatten-packages/bin/flatten" "$@"
  ret=$?
else 
  node  "$basedir/../flatten-packages/bin/flatten" "$@"
  ret=$?
fi
exit $ret
